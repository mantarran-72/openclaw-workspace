# 2026-07-02

- Rechecked Hermes VPS after Ethan pasted terminal output. `ssh hermes` from the desktop connects correctly to `ubuntu@hermes`; disk is resized and mounted correctly: `/dev/sda` 200G, `/dev/sda1` 199G ext4 mounted at `/`, root filesystem 193G with ~190G free. The `Permission denied (publickey)` in Ethan's paste came from running `ssh hermes` while already logged into `ubuntu@hermes`; on the VPS, `hermes` resolves to `127.0.1.1`, so it was trying to SSH into itself without the desktop private key. Server currently reports `*** System restart required ***`.
- Completed practical Hermes Agent install/config on the Oracle VPS. Hermes lives under `/home/ubuntu/.hermes`, version reported by doctor as `0.18.0`, Python venv is 3.11.15, config now uses OpenRouter with `anthropic/claude-sonnet-4.6`, and `~/.hermes/SOUL.md` was customized as a general-purpose assistant identity. Ran `hermes postinstall`, installed Ubuntu/Snap Chromium, exposed Hermes-managed Node 22.23.1/npm 10.9.8 via `/usr/local/bin` symlinks, initialized built-in skills (`hermes skills list`: 68 enabled), and confirmed doctor passes Node/agent-browser/Playwright Chromium. Remaining hard blocker: `OPENROUTER_API_KEY` is not set in `~/.hermes/.env`, so smoke test fails with "No LLM provider configured" until Ethan adds the key. Two old `hermes setup` wizard processes were left attached to pts/0 and pts/1 so Ethan can Ctrl-C his visible terminal if needed.
