# 2026-05-21

- Ethan contacted me by Telegram after reinstalling due to a computer crash.
- He said prior OpenClaw files were placed in `~/projects/openclaw-backup` and asked me to inspect them and load only necessary memory/context.
- Inspection found no filled prior identity/user/memory files in the backup workspace. It did find runtime/config files with credentials and device details, so those were intentionally not restored.
- Updated `USER.md` with Ethan's name, Telegram context, timezone, and the backup location.
- Created `MEMORY.md` with minimal verified continuity and restore guidance.
- Ethan asked whether the agent was still connected to GitHub/syncing. Initially checked current and backup `.git/config`; neither had remotes and `gh` was not found in PATH.
- Ethan provided `https://github.com/etarran-aerospect/openclaw-workspace`. Added it as `origin`, fetched `origin/main`, and found the missing prior `MEMORY.md`, `USER.md`, `IDENTITY.md`, and daily `memory/` notes there.
- Selectively restored continuity files from `origin/main`: `IDENTITY.md`, `USER.md`, `SOUL.md`, `TOOLS.md`, `HEARTBEAT.md`, `MEMORY.md`, `OPENCLAW-RECOVERY.md`, and `memory/`.
- Updated `MEMORY.md` to keep today's reinstall/recovery context and the warning not to import credentials/device auth/runtime config from the local backup without explicit need.
- Converted the workspace into a proper `main` checkout tracking `origin/main`, after first copying/moving the local recovery state to a timestamped backup under `~/projects/`.

## Later session notes

- Ethan added an external persistent memory folder under `~/AI` / `C:\AI` and said not to track the symlink in the OpenClaw workspace repo. Best setup: keep `AI` outside the workspace repo, track only a small config/README pointing to the memory location, and back up `AI` separately via GitHub/private repo, OneDrive, or manual zip backups.
- AeroSpect operating context from `AI/agents/aerospect-agent/startup-context.md` and referenced memory: Ethan wants focus on growing AeroSpect beyond vineyards and using his non-geo waiver as a business advantage for other verticals.
- AeroSpect baseline context remains: precision viticulture, drone imagery, thermal/multispectral analysis, GIS/orthomosaic workflows, but current strategy should emphasize new verticals enabled by waiver-backed drone operations rather than staying vineyard-only.
- Voice input test: Telegram/OpenClaw now provides machine-generated transcripts for inbound `.ogg` voice messages, so Ethan can talk-to-text to me without a separate Whisper setup. Two test messages transcribed as "Testing testing."
- WebODM startup guidance given: look for the local WebODM checkout and start it from that directory with its normal startup script, then open the local web UI in the browser. Do not assume Docker/WebODM is running until checked.
- QGIS plugin work: Ethan asked whether I can install QGIS plugins. Current machine has Windows/QGIS context in the error path. The Deepness plugin package install completed but validation failed with `ImportError: numpy.core.multiarray failed to import` under QGIS 3.4x / Python 3.12, likely a NumPy binary compatibility or mixed package install issue inside QGIS's Python environment. Future fix should target the QGIS Python environment specifically, checking `sys.executable`, `site-packages`, NumPy version, and reinstalling compatible wheels there rather than generic system Python.
