# 2026-07-23

## Shelina Pabani premium website pass
- Ethan wanted the Shelina Pabani website concept to feel premium and client-ready, with explanations moved out of the site copy and into a separate annotated screenshot.
- Updated `reports/shelina-pabani-premium-review-2026-07-22/` v8: tightened the hero/content language, kept the integrated SP monogram, used a smaller founder profile area, replaced uneven external client-logo images with a designed proof grid using controlled typed wordmarks/color cues, fixed mobile overflow, and generated desktop/mobile/full-page screenshots.
- Created explainer artifact `reports/shelina-pabani-premium-v8-callouts.png` plus SVG source, and packaged non-overwriting archive `reports/shelina-pabani-premium-review-2026-07-22-v8.tar.gz`.
- Revised the client-facing annotated screenshot after Ethan noted v8 used internal design-rationale language and a few arrows pointed to the wrong content. Created v9 assets with 10 client-safe callouts covering buyer positioning, enterprise/client proof, buyer paths, biography reframing, testimonial proof, and qualified inquiry. Final image: `reports/shelina-pabani-premium-v9-callouts.png`; archive: `reports/shelina-pabani-premium-review-2026-07-22-v9.tar.gz`.
- Published the Shelina preview under Bay Area Design Lab at `https://www.bayareadesignlab.com/previews/shelina-pabani` via GitHub commit `a151b164e5c98cb9ecdf0798915569e989922725` in `mantarran-72/bay-area-design-lab-ek`. Verified live HTTP 200, `x-robots-tag: noindex, nofollow`, expected Shelina page content, and a headless screenshot. Updated `email-draft-v9.md` with the live URL.

## Bay Area Design Lab contact webhook
- Hermes handoff said `badl-contact` webhook was live at temporary Cloudflare URL `https://ways-thesaurus-current-aid.trycloudflare.com/webhooks/badl-contact`, with secret redacted in chat as `P28864...uL4Y`; full secret was not available locally.
- Cloned `mantarran-72/bay-area-design-lab-ek` to `/tmp/bay-area-design-lab-ek`, changed `app/api/contact/route.ts` so the contact route reads `HERMES_WEBHOOK_URL` / `HERMES_WEBHOOK_SECRET` first and keeps `CONTACT_WEBHOOK_URL` / `CONTACT_WEBHOOK_SECRET` as fallback.
- Verified the route compiles with `./node_modules/.bin/next build`; `pnpm build` itself was blocked locally by pnpm 11 ignored-build-script policy, but the direct Next build succeeded.
- Pushed GitHub commit `e92e9f8450bdc5d20df03b962997264edf353a27` (`Support Hermes contact webhook env vars`) to `main`. Remaining blocker: set Vercel production env vars with full unredacted `HERMES_WEBHOOK_SECRET`, then redeploy/test a contact POST.
- Follow-up after locating Hermes handoff files on the VPS at `/home/ubuntu/website-sales/badl-webhook/`: the Hermes route expects a lean lead JSON payload signed with `X-Hub-Signature-256: sha256=<hmac>`, not a wrapped OpenClaw record. Updated BADL route accordingly and pushed commit `fa027072737771f94ae2c13cb4f817cd39b95c35` (`Match Hermes BADL webhook payload contract`) to `main`.
- Verified production `https://www.bayareadesignlab.com/api/contact` is serving the validation change by POSTing an incomplete form and getting HTTP 400 with `missing:["message"]`.
- Verified Hermes local webhook directly from the VPS with a signed BADL-shaped payload; Hermes returned HTTP 202 accepted for `/webhooks/badl-contact`. The old temporary Cloudflare URL `ways-thesaurus-current-aid.trycloudflare.com` no longer resolves, and public `http://129.146.53.190:8644/health` times out from desktop, so live Vercel-to-Hermes delivery still needs either Vercel env vars plus a stable reachable tunnel/domain, or Oracle firewall opening for port 8644.
- Ethan/Hermes opened the Oracle firewall for port 8644. Verified public `http://129.146.53.190:8644/health` returns 200 and a signed POST to `http://129.146.53.190:8644/webhooks/badl-contact` returns HTTP 202.
- Authenticated Vercel CLI via Ethan-approved device login as `mantarran`, linked `/tmp/bay-area-design-lab-ek` to Vercel project `mantarran/bay-area-design-lab`, and added Production env vars `HERMES_WEBHOOK_URL=http://129.146.53.190:8644/webhooks/badl-contact` and `HERMES_WEBHOOK_SECRET` (secret piped from Hermes VPS, not printed).
- Redeployed Production with Vercel deployment `dpl_ErBLegosYxSCGpoC4eq86n7S2PDF`, aliased to `https://www.bayareadesignlab.com`. Live POST test to `/api/contact` returned HTTP 200 and Vercel logs showed `BADL_CONTACT_LEAD_JSON` for `BADL Hermes Live Test`.
- Hermes gateway logs confirmed live Vercel delivery reached `badl-contact` from Node at `2026-07-23 18:15:23 UTC`, delivery ID `1784830523356`; Hermes completed the webhook agent response at `18:16:35 UTC` in 72.6s. Hound emitted warnings during the run, but the BADL form → Vercel → Hermes webhook path worked end to end.
- Final architecture clarification: the clean production path is `bayareadesignlab.com` form → Vercel `/api/contact` → Hermes `badl-contact` webhook → Telegram. The file-write and separate lead script are unnecessary for the current goal. Stable Hermes webhook URL is `http://129.146.53.190:8644/webhooks/badl-contact`; the webhook secret was provided in chat for Vercel env vars but should not be repeated in memory.

## All Tree Service preview review slider
- Updated the public All Tree Service preview at `https://all-tree-service-preview.ethan875743.chatgpt.site` with a before/after slider in the review highlights section.
- Source repo: `reports/all-tree-service-share-site/`; commit `20a225930b652c05d5ffd29b48bc989c344c0555` (`Add review before-after slider`); Sites version 4 deployed successfully.
- Added review assets `public/assets/reviews/oak-before.jpg` from Ethan's 2026-07-23 Telegram image and `public/assets/reviews/oak-after.jpg` from the existing All Tree assets. Live HTML and both live image assets returned HTTP 200 after deployment.
- Follow-up Telegram album image arrived separately. Replaced `public/assets/reviews/oak-after.jpg` with Ethan's second submitted image, committed `a6e2becc3170edec7149b5e42d54de6b758a5924` (`Use submitted after photo in review slider`), saved Sites version 5, and deployed it to the same preview URL. Live `oak-after.jpg` SHA-256 matched the second attachment after deployment.
- Ethan reported the bare preview link still showed no changes. Root cause: `/` was serving `index.html`, while the slider change was present in `/All_Tree_Service-website-preview.html`. Synced the updated slider HTML into `index.html` and `public/index.html`, rebuilt, committed `e72bfebc339f32d5896e20150d0c96535d3f0ad1` (`Serve review slider on root page`), saved Sites version 6, and deployed it. Verified the bare URL HTML contains `data-compare`, both oak image paths, and the enlarged review text; both review images returned HTTP 200.
- Follow-up label polish: Ethan wanted Before/After labels to disappear depending on which image side is visible. Updated the slider so `After` lives inside the clipped after-image layer and `Before` sits on the exposed before side; both root and preview HTML files are byte-identical. Commit `aca7babe7f13eeeb9ccbf4f8e529dc127fefd8a2` (`Clip slider labels to their image sides`), Sites version 7 deployed. Verified live root HTML has the new label structure and both review images return HTTP 200.
- Ethan reported the labels were still on the wrong images. Corrected the slider to the standard orientation: `oak-after.jpg` is the base/right-side image, `oak-before.jpg` is the clipped left overlay, `Before` is inside the before overlay, and `After` sits on the exposed after side. Commit `375803f7fe428ca050300e8a35c47afc0b3be08a` (`Put before label on before image side`), Sites version 8 deployed. Verified both cache-busted and plain live root URL HTML show the corrected `compare-before` structure and both images return HTTP 200.
- Final label/photo correction: actual files were semantically flipped, so markup-only fixes kept producing confusing labels. Added non-overwriting assets `review-before-correct.jpg` from Ethan's second/close messy tree attachment and `review-after-correct.jpg` from Ethan's first/farther cleaner attachment, updated root and preview HTML to use those names, committed `62ec1387561290e1501ed1de67f62ac5c1c71e2b` (`Use correct review before and after photos`), saved/deployed Sites version 9. Verified the live root HTML references `review-before-correct.jpg` with the `Before` label and `review-after-correct.jpg` with the `After` label; both live image hashes match the intended Telegram attachments.
- Review copy refinement: removed the Yelp Fusion API explanatory sentence, added `5.0 star rating on Yelp!` under `Review highlights.`, and added `Frank W. on Yelp` to the enlarged slider review. Commits `3306e29f39facf06b516dd2192253fab7b3bcba2` and packaging commit `af0bf4b2fa634286b1af980a92f330a94f9b27df`; Sites version 11 deployed. Verified live root and direct HTML contain the new rating/name and no longer contain the removed sentence.
- Logo replacement: Ethan supplied a cleaner All Tree Service logo image. Converted it to `public/assets/all-tree-logo-transparent.png` by flood-removing only edge-connected white background into PNG alpha, replaced the embedded base64 logo in `index.html`, `public/index.html`, and `public/All_Tree_Service-website-preview.html`, enlarged the nav logo slot to `70x56`, committed `25e340cc82cd0db97c04bc8193bde34cf92aaad4` (`Use transparent logo asset`), saved Sites version 12, and deployed it to `https://all-tree-service-preview.ethan875743.chatgpt.site`. Verified live root HTML references the PNG once, no embedded logo remains, Yelp/Frank review text persists, and the live PNG hash matches local.
- Pre-compaction note: for future All Tree Service preview work, the canonical deployable source used today was `reports/all-tree-service-share-site/`. The important lesson from the "no changes" report is to update/sync the root `index.html` path before saving a Sites version, because Ethan checks the bare preview URL.

## All Tree Service copy cleanup (post-compaction)
- Applied Ethan's final copy pass to `reports/all-tree-service-share-site/index.html`, `public/index.html`, and `public/All_Tree_Service-website-preview.html`: removed `Real All Tree Service photos from Yelp make the site feel local and credible fast.`, changed the gallery heading `Recent tree work and cleanup.` to `Recent tree work from around the North Bay.`, removed the trailing `Yelp lists the business as open now with 24/7 availability and a 5.0-star rating from local customers.` from the hero paragraph, and changed `The service-area screenshot covers Santa Rosa...` to `Our service-area covers Santa Rosa...`. Local commit `36e5bef` (`Refine hero, gallery, and service-area copy`).
- Deploy blocker: `git push sites main` to `https://git.chatgpt-team.site/.../appgprj_6a6024f9e4448191a71218b97ca34b40.git` returned `Authentication required`. Only cached credential in `~/.git-credentials` is for GitHub; no sites token found under `~`, no ChatGPT sites CLI installed, no browser MCP `user` profile attached. Prior successful pushes today (versions 4–12) must have relied on interactive prompts or an ephemeral credential source that did not persist across compaction. Notified Ethan; source changes are committed locally and just need a sites-remote credential to push.
