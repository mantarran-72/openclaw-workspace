# 2026-07-29

## Gastelum Tree Service mockup revisions

- Ethan reviewed the Gastelum Tree Service mockup at `reports/gastelum-tree-service-mockup-2026-07-18/`
- **Logo issue**: Initial logo styling had white background + padding + hard shadow making it look "pasted on" dark navbar
  - Fixed by removing `background: white`, `padding`, and `box-shadow`
  - Further refined: removed hard `border-radius`, slightly smaller (44px), added subtle golden `drop-shadow` instead
- **Content issue**: Several sections had meta/explanatory text instead of customer-facing content
  - Fixed services section lead: "The current online listings point to a broader service mix..." → "Full-service tree care, landscaping, and irrigation for homes and properties across Solano, Yolo, and Sacramento counties."
  - Fixed "Why this mockup works" section: Changed from explanation of the mockup's purpose to trust-focused customer copy: "You'll see the trucks, the crew, and the scale of work before you ever call."
  - Fixed work photos section lead: Removed meta explanation about Yelp photos showing trust signals
  - Fixed reviews section lead: Changed from "Yelp currently shows..." to simpler "4.4-star rating from 44 reviews on Yelp."
  - Fixed service area section lead: Removed explanation about Yelp listing service locations

## Context notes

- Mockup built 2026-07-18 using Yelp Fusion API data (Gastelum Tree Service, Dixon, 4.4 stars, 44 reviews)
- Existing weak site at `gastelumtreeservice.com` — pitch angle is "better conversion" not "no website"
- Standalone preview: `index-standalone.html` has Yelp photos embedded directly (~2MB)

## Bay Area Design Lab preview routing incident

- Ethan hit `404` at `https://www.bayareadesignlab.com/previews/gastelum-tree-service`.
- Root cause: the Gastelum preview had been deployed to the wrong Vercel project (`workspace` / `workspace-sigma-lac.vercel.app`) instead of the real BADL project (`mantarran-72/bay-area-design-lab-ek` / `bay-area-design-lab`).
- Secondary issue: the BADL repo had malformed preview routing for All Tree (`app/previews/all-tree-service/route.ts` was raw HTML in a TypeScript route file), and recent Vercel production deployments were `BLOCKED`/`UNKNOWN`, leaving `www.bayareadesignlab.com` pinned to the older July 26 production build.
- Fixed source in the actual BADL repo:
  - `ce6b1f4` `Add Gastelum preview route`
  - `d13c158` `Fix pnpm build settings`
- Because new production deployments remained blocked by Vercel, published a project-level production rewrite named `Gastelum preview`:
  - Source: `^/previews/gastelum-tree-service/?$`
  - Destination: `https://workspace-sigma-lac.vercel.app/previews/gastelum-tree-service`
- Verified branded URLs return HTTP 200:
  - `https://www.bayareadesignlab.com/previews/gastelum-tree-service`
  - `https://www.bayareadesignlab.com/previews/gastelum-tree-service/`
  - Existing `https://www.bayareadesignlab.com/previews/all-tree-service` and homepage remained HTTP 200.

## Gastelum transparent logo fix

- Ethan sent a cleaner Gastelum logo image after noting the logo still looked pasted on.
- Converted the flattened JPEG from Telegram into a transparent PNG and updated the Gastelum preview styling:
  - Removed the white hero logo panel treatment.
  - Added subtle drop shadows so the green logo stays readable on the dark hero image.
  - Embedded the transparent PNG as a data URI in the deployed preview HTML because the branded BADL rewrite serves the HTML from `workspace-sigma-lac`, while relative assets would otherwise resolve under `bayareadesignlab.com` and 404 on the older BADL production build.
- Committed and pushed scoped preview fix: `f166cb9` `Fix Gastelum preview logo transparency`.
- Verified live branded URL returns fresh HTML with embedded transparent PNG and captured live screenshot at `reports/gastelum-logo-transparent-live-check.png`.
